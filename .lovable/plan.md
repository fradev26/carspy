Voeg linksboven op `/auth` (in `src/pages/Auth.tsx`, in de `Auth`-component die de `Card` rendert) een ronde ghost-button met `ArrowLeft` (lucide-react) toe. Op klik: `navigate(-1)` als er geschiedenis is, anders `navigate('/')`. Plaatsing: `absolute top-4 left-4` op de buitenste `<div>` (die wordt `relative`), met `aria-label="Terug"` en `.focus-ring`. Geen andere wijzigingen.